# frozen_string_literal: true

RSpec.describe Bleesed do
  it "has a version number" do
    expect(Bleesed::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
